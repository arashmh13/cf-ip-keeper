[Unit]
Description=CF IP relay scanner (continuous)
After=network-online.target

[Service]
WorkingDirectory=/opt/cf-ip-keeper
ExecStart=/bin/bash /opt/cf-ip-keeper/scan_daemon.sh
Restart=always
RestartSec=10
Nice=10

[Install]
WantedBy=multi-user.target
